# @ctrlcan/orbit

Cross-page, themeable React site tour.

## Install
```bash
npm install @ctrlcan/orbit
Usage
jsx
Kodu kopyala
import { CtrlcanOrbit } from '@ctrlcan/orbit';

function App() {
  return <CtrlcanOrbit steps={[]} />;
}
yaml
Kodu kopyala

---

### Git Commit

```bash
git add package.json vite.config.js .eslintrc.cjs .npmignore README.md
git commit -m "chore(config): add base package.json, vite, eslint, npmignore, and README"